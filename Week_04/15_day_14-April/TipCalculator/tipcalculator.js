    //var billAmount = 0 ; 
    //var tipRate = 0;
    //var tip = 0;
    //var totalAmount = 0;
    //var excludeVAT = 18;
    //var billAmountExVAT = 0;
    //billAmount = prompt("Please enter your bill amount as number, e.g. 230.50");
    //tipRate = prompt("Please enter the tip rate as number, e.g. 15%");
    //excludeVAT = prompt("Do you want to exlude the VAT (18%)");

    var billAmount = Number (prompt("Please enter your bill amount as number, e.g. 230.50"));
    var tipRate = Number (prompt("Please enter the tip rate as number, e.g. 15%"));
    var excludeVAT = Number (prompt("Do you want to exlude the VAT (18%)"));


    if (Number.billAmount == integer){
         document.write(" The Number " + Number.billAmount + "is integer");
    }else{
        document.write(" The Number " + Number.billAmount + "is decimal"); 
    }
    if (Number.tipRate == integer){
        document.write(" The Number " + Number.tipRate + "is integer");
   }else{
       document.write(" The Number " + Number.tipRate + "is decimal"); 
   }

   Math.round();
    

    if (excludeVAT == yes){
        document.write(" The Number " + billAmountExVAT + "is equal to" + billAmount + "-" (+ billAmount + "*" + excludeVat + "/" + 100 + "")); 

    }
    
    var billAmount = Number (prompt("What is the bill amount?"));
    var tipRate = Number (prompt("What is the tip rate?"));
    var tip = Number(billAmount) * ( Number (tipRate) / 100);

    var total = Number (billAmount) + ( Number(billAmount) * ( Number (tipRate) / 100) );

    window.alert()

  
 

   